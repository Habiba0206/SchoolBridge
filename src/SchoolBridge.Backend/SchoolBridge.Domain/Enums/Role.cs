namespace SchoolBridge.Domain.Enums;

/// <summary>
/// Represents different roles that users can have in the system.
/// </summary>
public enum Role
{
    /// <summary>
    /// student role for users
    /// </summary>
    Student = 0,

    /// <summary>
    /// teacher roles for users
    /// </summary>
    Teacher = 1,
}
